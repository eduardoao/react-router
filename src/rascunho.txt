// {
//         this.props.data.nav.map((id, name, path) => {
//             let DynamicComponent = require('./' + id + '/' + name).default;
//             return <Route key={index} path={item.route.url}
//                           exact={item.route.exact}
//                           render={() => (<DynamicComponent key={path}/>)}/>
//           }}  


 {/* this.state.data.map(
               (data=> <Route exact path={data.path} component={data.name} /> )
            )     */}